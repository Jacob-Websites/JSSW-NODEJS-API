name: Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Trigger Render Deploy
      run: curl -X POST "https://api.render.com/deploy/srv-cq56eltds78s73csoqog?key=VU1j0G3YeZk"
